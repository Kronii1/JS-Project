id를 가져오는방법

document.getElementById("해당아이디")

document.getElementById(“title”)
은 html에 있는 id를 불러올 수 있다.

js에서는 html이 표현하는 object를 보여준다

title.innerText = “got you”
처럼 js에서 id를 추가했기에 가능하다.

모든 것들을 html에서 항목들을 가지고와서 js에서 변경한다.
console.dir(“title”);
js에서는 html이 표현하는 object를 보여준다.

모든 것들을 html에서 항목들을 가지고와서 js에서 변경한다.